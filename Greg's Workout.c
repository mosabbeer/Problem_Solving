#include<stdio.h>
int main()
{
    int n,a[100],i;
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        scnaf("%d",&a[i]);
        if(i%3==0)
            back=back+a[i];
        else if(i%2==)

    }

}
